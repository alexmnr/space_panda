# space_panda
Turning a franka panda into a force-feedback spacemouse

# Installation and Build
Clone the repository into your src diretory of the ros2 workspace you are using:
```bash
git clone https://github.com/alexmnr/space_panda.git
cd space_panda
git submodule update --init --recursive
```
